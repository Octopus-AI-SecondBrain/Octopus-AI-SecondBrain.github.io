# Production Environment Variables Template for Render.com
# Copy these to your Render service environment variables

# REQUIRED - Database (Render PostgreSQL)
DATABASE_URL=postgresql://user:password@host:port/dbname

# REQUIRED - Security (Generate a strong 32+ character secret)
SECRET_KEY=your-very-long-secret-key-here-at-least-32-characters

# REQUIRED - OpenAI (for embeddings)
OPENAI_API_KEY=sk-your-openai-api-key

# Application Settings
ENVIRONMENT=production
DEBUG=false
ENABLE_HTTPS=true
LOG_LEVEL=INFO

# CORS Settings (add your Render backend URL and GitHub Pages origin)
# Example: CORS_ORIGINS=https://octopus-ai-secondbrain.github.io,https://your-render-app.onrender.com
CORS_ORIGINS=https://octopus-ai-secondbrain.github.io

# Server Settings (Render will set PORT automatically)
HOST=0.0.0.0