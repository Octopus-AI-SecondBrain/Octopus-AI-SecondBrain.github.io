# SecondBrain Environment Configuration Template
# Copy this to .env and update values for your environment

# ===== DEPLOYMENT ENVIRONMENT =====
# Set to: development, staging, or production
ENVIRONMENT=development

# Enable debug mode (development only)
DEBUG=true

# Enable HTTPS features (required for production cross-origin cookies)
ENABLE_HTTPS=false

# ===== SECURITY =====
# CRITICAL: Generate a strong secret key for production!
# Run: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=dev-secret-change-me-for-production

# JWT token expiration in minutes
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ===== DATABASE =====
# Development: SQLite (default)
DATABASE_URL=sqlite:///./data/database/secondbrain.db

# Production: PostgreSQL (recommended)
# DATABASE_URL=postgresql://user:password@host:port/database

# Enable query logging (development only)
DATABASE_ECHO=false

# ===== CORS CONFIGURATION =====
# Allowed frontend origins (comma-separated, no trailing slashes)
# For development, these defaults should work:
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:4173,http://127.0.0.1:3000,http://127.0.0.1:5173,http://127.0.0.1:4173

# Production: Add your GitHub Pages URL
# CORS_ORIGINS=https://yourusername.github.io,http://localhost:5173

# ===== VECTOR STORE =====
# ChromaDB storage path
CHROMA_PATH=./data/vector_db

# ===== OPENAI (OPTIONAL) =====
# OpenAI API key for enhanced embeddings (leave empty to use default embeddings)
OPENAI_API_KEY=

# ===== EMAIL CONFIGURATION (FUTURE) =====
# Email sender address for account recovery and notifications
# Currently not used, but reserved for future transactional email features
DEFAULT_FROM_EMAIL=noreply@yourdomain.com

# ===== LOGGING =====
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL  
LOG_LEVEL=INFO

# Enable JSON structured logging (production)
ENABLE_JSON_LOGGING=false

# Log file path (optional, logs to stdout if empty)
LOG_FILE=

# ===== SERVER CONFIGURATION =====
# Server host and port (for development)
HOST=127.0.0.1
PORT=8000

# ===== FRONTEND CONFIGURATION =====
# These are used by the React frontend (prefix with VITE_)

# Backend API URL - MUST match your backend deployment
VITE_API_URL=http://localhost:8000

# Base path for frontend routing (GitHub Pages: /repository-name/, custom domain: /)
VITE_BASE_PATH=/

# ===== PRODUCTION DEPLOYMENT URLS =====
# These are used for CORS and security configuration

# Your GitHub Pages frontend URL (no trailing slash)
GITHUB_PAGES_URL=https://yourusername.github.io

# Your GitHub Pages hostname
GITHUB_PAGES_HOST=yourusername.github.io

# Your Render backend URL (auto-detected, but can be overridden)
# RENDER_EXTERNAL_URL=https://your-app.onrender.com